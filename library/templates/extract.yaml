version: "0.1"
name: extract

# Extract mode: turn a folder of source documents (transcripts, RFPs, notes) into
# one consolidated requirements document. Each source is extracted on its own, then
# a writer synthesizes them and a critic reviews the draft until it is fit to ship.

nodes:
  - id: scan
    type: builtin/scanner

  - id: extract
    type: agent
    agent: source_processor@1
    map: scan.sources
    params: { workers: 3, min_ok: 1 }

  - id: refine
    type: loop
    params: { max_rounds: 3 }
    body:
      agent: requirements_writer@1
      inputs: { extracts: extract.extract }
    critic:
      agent: requirements_critic@1
      inputs: { draft: "@body", extracts: extract.extract }
    outputs: { doc: "@body" }
