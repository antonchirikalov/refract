"""Input/output materialization, collections, gates (SPEC §10.1, §10.3–§10.4).

Per-port subdirectories, collection manifests, gate validation against the
registry. All linking goes through the single link_or_copy() helper.
"""
