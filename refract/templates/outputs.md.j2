## Output

Produce exactly the following artifact(s) by **writing each one to its path with
your file-writing tool**. Do NOT print artifact contents in your reply — only the
files you write are read, and your reply text is discarded. Write nothing else to
the output directory.
{% for o in outputs %}
### `{{ o.port }}` ({{ o.type }}){% if o.optional %} — optional{% endif %}

Write to: `{{ o.path }}`
{{ o.summary }}
{% endfor %}